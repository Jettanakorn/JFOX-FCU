# Vendored upstream files are kept byte-identical for traceability - their
# READMEs assert exact upstream sizes, which line-ending conversion would
# silently falsify. Do not normalize these.
hardware/vendor/PX4FMUv2.4.5.sch -text
px_mkfw.py                       -text

# KiCad schematics are s-expression text but are machine-generated by
# hardware/tools/gen_tmr_schematic.py; leave them alone so a regenerate and a
# checkout compare equal.
*.kicad_sch -text
*.kicad_pcb -text
*.kicad_sym -text
